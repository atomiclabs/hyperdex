version: 2
jobs:
  build:
    macos:
      xcode: '9.2.0'
    steps:
      - checkout
      - run: yarn
      - run: yarn test
      - run: yarn run dist
workflows:
  version: 2
  build:
    jobs:
      - build
